rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This allows any client to read and write to the entire database.
    // This is for debugging only and is not secure.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
