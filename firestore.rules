rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents for debugging.
    // WARNING: This is insecure and should be replaced with secure rules
    // before deploying the application.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
